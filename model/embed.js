class EmbedHolder {
  constructor(urls = [], textContent = '', username = '', profilePicture = '') {
    this.URLs = urls;
    this.textContent = textContent;
    this.username = username
    this.profilePicture = profilePicture
  }

  // Add methods as needed
}

export default EmbedHolder;
